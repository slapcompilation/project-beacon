# AIP / Foundry reference images

Drop Foundry / AIP screenshots here — the visual ground truth the parity gate in
[`../AIP-UX-RESTRUCTURE.md`](../AIP-UX-RESTRUCTURE.md) checks each Beacon surface
against.

Source: the Foundry getting-started / AIP docs
(`palantir.com/docs/foundry/getting-started/orientation-and-nav/` and siblings).

## Naming

One image per parity-board surface, named for the surface so the board can link
it directly:

```
shell-sidebar.png          # the 5-section left sidebar
shell-quicksearch.png      # jump-to + full-results modes
home.png                   # customizable landing
object-view.png            # star + metadata + sidebar shortcuts
applications-portal.png
logic-canvas.png           # block canvas + debugger
agent-studio.png
evals-diff.png             # cases + version diff + cohorts
modeling-lifecycle.png
ontology-manager.png
```

Add more as phases need them; keep the name = the surface it grounds.
