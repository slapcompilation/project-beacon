---
verify: strict
---

# Vertex — the system graph, and the four vocabularies underneath it

**Pages read whole: 28 of 28.** `vertex/_index.md`, `overview.md`,
`example-use-cases.md`, `explore-object-relationships.md`, `graphs-explore.md`,
`graphs-display-options.md`, `graphs-template.md`, `generate-graph-functions.md`,
`generate-graph-apps.md`, `derive-property-functions.md`, `link-merging.md`,
`events-overview.md`, `configure-events.md`, `configure-thresholds.md`,
`explore-related-events.md`, `explore-time-series.md`, `use-time-selection.md`,
`timeline.md`, `scenarios-overview.md`, `scenarios-getting-started.md`,
`scenarios-options.md`, `chained-models.md`, `media-layers.md`,
`embed-graph-workshop.md`, `save-share.md`, `read-only-mode.md`,
`vertex-settings-control-panel.md`, `marketplace-vertex.md`.

**Images: the section holds 150. Every one is referenced by a page and every
referenced one is on disk — I cross-checked both directions and both lists came
back empty, so nothing here is a dangling `./media/` reference. I opened 36.
The other 114 I did not open, and they are named below so the debt is recorded
as mine rather than left implied.**

The ones I opened:
`control-panel-vertex.png`, `styling_layout_1.png`,
`optional_ontology_config-event.jpg`, `optional_ontology_config-link_merge.png`,
`optional_ontology_config-threshold.jpg`,
`optional_ontology_config-edge-direction.jpg`,
`template-configure-parameters.png`, `template-configure-search-arounds.png`,
`template-configure-layers.png`, `template-summary.png`,
`graph_generation_and_search_around-ui.jpg`,
`graph_generation_and_search_around-parameters.jpg`,
`graph_generation_and_search_around-troubleshoot.jpg`, `explore_objects_4.jpg`,
`explore_objects_5.png`, `explore_objects_9.png`, `explore_objects_12.png`,
`workshop-input-section.png`, `workshop-interaction-section.png`,
`workshop-interface-section.png`, `workshop-capabilities-section.png`,
`workshop-time-configuration.png`, `timeline_shape-menu.png`,
`timeline_overview-expanded.png`, `timeline_color-menu.png`,
`timeline_playback_controls.png`, `media-layers-config-ship-diagram.png`,
`media-layers-filter-object.png`, `media-layers-search-dialog.png`,
`saved-selection-layers-panel-and-borders.png`, `graph_history.png`,
`simulate-system-12.jpg`, `explore-existing-6.jpg`,
`save_share_collaborate-draft.jpg`, `styling_node_type.jpg`,
`styling_line_type.jpg`.

The ones I did **not** open — the skips are mine, not gaps in the corpus:
`changes-over-time-1.jpg`, `changes-over-time-2.jpg`, `changes-over-time-3.jpg`,
`changes-over-time-4.jpg`, `changes-over-time-5.jpg`, `double-chevron.png`,
`enable_versioning.png`, `explore-events-1.jpg`, `explore-events-2.jpg`,
`explore-events-3.jpg`, `explore-events-4.jpg`, `explore-events-5.jpg`,
`explore-existing-1.jpg`, `explore-existing-2.jpg`, `explore-existing-3.jpg`,
`explore-existing-4.jpg`, `explore-existing-5.jpg`, `explore-existing-7.jpg`,
`explore-existing-8.jpg`, `explore_objects_1.jpg`, `explore_objects_10.png`,
`explore_objects_11.png`, `explore_objects_13.png`, `explore_objects_14.jpg`,
`explore_objects_2.jpg`, `explore_objects_3.jpg`, `explore_objects_6.png`,
`explore_objects_7.png`, `explore_objects_8.png`, `eye-open.svg`,
`generate_graph_from_other_app-ui.jpg`, `media-layers-action-form.png`,
`media-layers-action-options.png`, `media-layers-add-object-result.png`,
`media-layers-add-object.png`, `media-layers-add-to-graph.png`,
`media-layers-annotation-example-data.png`, `media-layers-bounding-box.png`,
`media-layers-config-annotation.png`, `media-layers-create-annotation.png`,
`media-layers-diagram-example-data.png`, `media-layers-diagram-properties.png`,
`media-layers-draw-annotation.png`, `media-layers-edit-form.png`,
`media-layers-edit-result.png`, `media-layers-end-result.png`,
`media-layers-linen-locker-edit.png`,
`media-layers-linen-locker-existing-object.png`, `media-layers-link-key.png`,
`media-layers-media-example-data.png`,
`media-layers-oma-annotation-action-types.png`, `media-layers-oma-annotation.png`,
`media-layers-oma-create-annotation-action.png`,
`media-layers-ontology-overview.png`, `media-layers-search-around.png`,
`media-layers-selected-object.png`,
`media-layers-ship-annotation-properties.png`, `media-layers-style-icon.png`,
`media-layers-style-option.png`, `media-layers-style-result.png`, `more.svg`,
`optional_ontology_config-link_merge2.jpg`,
`save_share_collaborate-duplicate.jpg`, `save_share_collaborate-save.jpg`,
`saved-selection-edit-objects.png`, `saved-selection-more-actions.png`,
`saved-selection-quick-select.png`,
`saved-selection-save-objects-in-selection.png`,
`search_around_functions-all_flights.jpg`,
`search_around_functions-destinations.jpg`, `simulate-system-1.jpg`,
`simulate-system-2.jpg`, `simulate-system-3.jpg`, `simulate-system-4.jpg`,
`simulate-system-5.jpg`, `simulate-system-6.jpg`, `simulate-system-7.jpg`,
`simulate-system-8.jpg`, `simulate-system-9.jpg`, `simulate-system-10.jpg`,
`simulate-system-11.jpg`, `simulate-system-13.jpg`, `styling_badges.jpg`,
`styling_color_by.jpg`, `styling_edge_badges.jpg`, `styling_edge_styling.jpg`,
`styling_extended_labels_1.jpg`, `styling_group_by_property.jpg`,
`styling_group_edge.jpg`, `styling_group_nodes_1.jpg`, `styling_label.jpg`,
`styling_line_color.jpg`, `styling_object_styling_panel.jpg`,
`styling_saved_styles.jpg`, `styling_subtitle.jpg`, `styling_width.png`,
`template-helper.png`, `template-source-graph.png`, `template-usage.gif`,
`timeline_bar-events.png`, `timeline_by-object-type.png`,
`timeline_color-by-property.png`, `timeline_cursor-edit.png`,
`timeline_cursor-pos.png`, `timeline_diamond-select-time-property.png`,
`timeline_event-lines.png`, `timeline_graph-time-filter.png`,
`timeline_playback_with_filter.png`, `timeline_timeline-button.png`,
`timeline_zoom-to-fit.png`, `vertex_overview-system_graph.jpg`,
`vertex_use_cases-production_diagram.jpg`, `vertex_use_cases-system_graph.jpg`,
`workshop-adding-widget.png`.

**Sublinks followed outside `vertex/`:** I read
`object-link-types/metadata-typeclasses.md` (the page that LISTS the `vertex`
type-class set) and the relevant part of `map/selection.md` (which turns out to
carry an enumeration the Vertex pages never print). I did **not** read
`time-series/`, `functions/`, `action-types/`, `model-integration/`, the Notepad
Vertex-graph widget page, the Carbon overview, or `devops/` — all linked from
these pages, all left for their own readings. Notepad and Carbon are among the
sections that are absent from the mirror entirely, so those two links go
nowhere on disk.

---

## 1. Is Vertex sunset? — no, but two of its features are, and the distinction matters

Asked explicitly because the task asked it explicitly. **The Vertex product
carries no sunset banner anywhere in the section.** `overview.md` and `_index.md`
are identical, and neither has a callout of any kind. I grepped the whole
section for `sunset`, `deprecat`, `legacy`, `end of life`, `no longer`, `retire`,
`will be removed` and `superseded`; six lines came back, and not one of them is
about Vertex as a whole.

What *is* sunset is **the model half of scenarios**, on two pages:

> Model chaining in Vertex is in the sunset phase of development and will be deprecated at a future date. Full support remains available. To continue using models in Vertex scenarios, we recommend configuring a function for the model and importing that function into a function-backed Action.

— `vertex/chained-models.md`

> The model selection, configuration, and run process described below is in the sunset phase of development and will be deprecated at a future date. Full support remains available.

— `vertex/scenarios-getting-started.md`

A third, softer signal in Control Panel says the same thing without the word:

> **Legacy model configuration:** An updated model configuration experience is available through the Modeling objectives application. While support for models configured directly through Vertex will remain, configuring new models in the Vertex configuration panel is now discouraged.

— `vertex/vertex-settings-control-panel.md`

And one *forward*-looking marker, in the opposite direction — a feature in beta,
not sunset:

> Diagramming in Vertex is in the beta phase of development and may not be available on your enrollment. Functionality may change during active development. To enable this feature, contact your platform administrator to modify Vertex settings in Control Panel.

— `vertex/example-use-cases.md`

So the section's own lifecycle reading is: **the graph is current, diagramming is
beta, the model-selection UI is sunset, and the replacement path is
Function-on-model → function-backed Action → an Action inside a scenario.** The
one remaining `legacy` hit is unrelated — a Workshop input mode, quoted in §9.

That replacement path is the single most important thing in this section for us,
because it moves Vertex's simulation engine off *Vertex knows about models* and
onto *Vertex runs Actions*, and we already have Actions.

---

## 2. What Vertex is

> Vertex allows you to visualize and quantify cause and effect across the digital twin of your real-world organization. Using the Vertex toolkit, you can access and explore existing graphs that have been curated and published by your organization or build new system graphs.

— `vertex/overview.md`

> Informing data-driven decisions based on current, predicted, or proposed conditions, Vertex provides a toolkit to monitor, simulate, and optimize operational decision-making to maximize organizational outcomes.

— `vertex/overview.md`

The four bullets under that — transparency, focus, simulate, learn — are
marketing framing and I am not treating them as structure.

`example-use-cases.md` splits the product in two: **point-and-click process flow
diagramming** (beta) and **simulation, dynamic analysis, and decision
optimization**. It names two visual forms, *production diagrams* and *system
graphs*, and says of the second half:

> Vertex allows you to interact and interrogate your digital twin through integration with any model, forecast, or business logic published in Foundry.

— `vertex/example-use-cases.md`

`explore-object-relationships.md` places it in the platform:

> Vertex can be launched from the Foundry workspace sidebar's **Apps** section, under the **Operational Applications** header.

— `vertex/explore-object-relationships.md`

**Vertex has no store.** Nothing in 28 pages says Vertex owns data. It reads
object types, link types, events, time series and Actions out of the Ontology,
and it writes exactly two things back: a saved **graph** resource and a saved
**graph template** resource (plus **saved Search Arounds**, §4). That is the same
shape as Quiver, and it is the shape that matters for a Postgres build.

---

## 3. The application shell — almost entirely from screenshots

The prose never describes the window. Four captures do, and they disagree with
each other in ways that read as two eras.

**The full application chrome**, from the version-history capture:

> Vertex › Flights · File · View · Help · Model: None · default • Latest Data · Save · Share
> — vertex/images/graph_history.png

That capture also shows a right-hand rail of four vertical tabs — **Model
config, Simulation, Search Around, Graph history** — with Graph history open and
holding two entries:

> v2 · Modified color · now — v1 · ⟲ Revert · 43 seconds ago
> — vertex/images/graph_history.png

Nothing in `save-share.md` says the version history lives in that rail, or that a
version carries a free-text label like `Modified color`. Both come only from the
image.

**The toolbar**, from the layout capture, reads left to right:

> Selection ▾ · Search Around ▾ · Group ▾ · Layout ▾ · Annotate ▾ · Draw · Delete | Undo · Redo | ?
> — vertex/images/styling_layout_1.png

`Draw` and `Annotate` are in no sentence on any of the 28 pages. Neither is the
left rail that the same capture shows: **Layers, Selection, Search, Histogram,
Info** — five tabs. The prose mentions Selection, Events, Histogram and Layers
tabs scattered across four pages and never assembles the set.

And the set is not stable. The Workshop-embedded exploration capture shows
**four** left tabs, with `Info` absent, plus a `Time selection` button and a
banner-level `OBJECTS SELECTOR` chip:

> Layers · Selection · Search · Histogram — To see object details or styling controls, select objects on the graph
> — vertex/images/explore-existing-6.jpg

I am marking that as inference rather than a documented difference: it is
consistent with the embed hiding panels (§9 shows every panel is a toggle), but
it is also consistent with an older capture, and no sentence adjudicates.

The **layer list** is the one structure the images make unambiguous. Layers are
grouped into `NODES` and `EDGES`, one row per object type and one per link type,
each row carrying a count and the colouring rule:

> NODES — [Example Data] Airport · 3 nodes, colored by Airport — [Example Data] Flight · 3 nodes, colored by object type · EDGES — [Example Data] Flight ↔ [Example Data] Air… · 6 edges
> — vertex/images/styling_node_type.jpg

Three distinct colouring modes appear across captures — `colored by object type`,
`colored by fixed color`, and `colored manually` — plus `colored by <property>`
with a rendered legend (a purple/green/blue band labelled SFO / DFW / SAN for a
categorical property; a blue-to-red gradient labelled `0`…`60` for a numeric
one). The prose's `Color by` section names none of these tokens.

A third layer section exists that the prose does mention but does not show is a
peer of the other two:

> SAVED SELECTIONS — Affected Flights to Triage · 2 objects — High Priority · 1 object — Important Potentially Affected Routes · 1 object
> — vertex/images/saved-selection-layers-panel-and-borders.png

`graphs-display-options.md` says a saved selection has a name and a colour. The
image adds that it also carries a **live object count**, and that it sits as a
third top-level section of the layers panel beside NODES and EDGES.

### The styling menus, and their exact section sets

The node styling menu has **seven** sections. The prose lists five.

> FILL COLOR · SUBTITLE · EXTENDED LABELS · BADGES · NODE STYLE · TIMELINE SHAPE · TIMELINE COLOR
> — vertex/images/timeline_shape-menu.png

`graphs-display-options.md` documents Color by, Subtitle, Extended labels,
Badges and Node style; `timeline.md` documents Timeline shape and Timeline
colour as if they were a separate feature. **They are the same menu.** That is
only visible in the image, and it is the kind of thing that would otherwise be
built as two panels.

The edge styling menu has **five** sections and **two** trailing toggles, and it
is headed by the link type's two ends drawn as a directed pair:

> [Example Data] Airline ↓ [Example Data] Route — LINE COLOR · LINE TYPE · WIDTH · LABEL · BADGES — Show arrows · Show edges reversed
> — vertex/images/optional_ontology_config-edge-direction.jpg

`Show arrows` and `Show edges reversed` are the per-graph controls the prose
alludes to in one clause and never names.

Two small enumerations are settled by image and only approximated in prose. Node
style:

> Source — Default · Default node style — Object Card · A more detailed card showing details of an object
> — vertex/images/styling_node_type.jpg

Line type:

> Type — Straight · Straight edges between two nodes — Curved · Curved edges between two nodes — Orthogonal · Right-angled edges between two nodes
> — vertex/images/styling_line_type.jpg

### Layout: two enumerations that do not match

The layout dropdown lists **ten** entries, and the gear icon is present on eight
of them:

> Auto · Hierarchy (Left to Right) · Hierarchy (Top to Bottom) · Grid · Horizontal Row · Vertical Column · Circular · Radial · Cluster · Cartesian
> — vertex/images/styling_layout_1.png

That corroborates the prose exactly:

> Below are the advanced parameters for each layout. Note that **Auto** and **Circular** layouts do not have advanced parameters.

— `vertex/graphs-display-options.md`

But `generate-graph-functions.md` gives a *different* set for the same concept,
as a wire value a function returns:

> `layout` allows you to alter the layout which the resulting objects use when being added to the graph. The provided value must be one of `auto`, `auto-grid`, `grid`, `linear-row`, `linear-column`, or `circular`. Defaults to a hierarchy layout if omitted.

— `vertex/generate-graph-functions.md`

**Six programmatic values against ten UI entries.** `auto-grid` has no UI entry;
Hierarchy, Radial, Cluster and Cartesian have no wire value — even though
hierarchy is what you get by *omitting* the field. This is the two-vocabularies
trap CLAUDE.md warns about, in a section we have not built yet, and I am
recording it rather than picking one. See the Decisions block.

---

## 4. Search Around — the traversal engine, and the vocabulary it uses

This is the heart of the product and the part closest to something we already
have (`object_set_where`, traversal, link types).

### Basic Search Around

> Select from the list of related objects to add them to the graph. The total number of objects of each type is shown within the dropdown list. With a large number of objects, you should consider using the filter icon next to the number of objects to include additional filters.

— `vertex/explore-object-relationships.md`

The right-click menu is fully enumerated only in images, and the two captures
name the same submenu differently. The newer one:

> Search Around ▸ · Open linked events ▸ · Open series ▸ · Edit styling · Select all ⌘A · Select all "[example data] airports" ⌘⇧A · Invert selection ⌘I · Select linked objects ▸ · Filter selected objects · Open in… ▸ · View data lineage · Delete selection
> — vertex/images/explore_objects_4.jpg

with the submenu headed by the object type and each relation carrying a count and
its own filter icon:

> [EXAMPLE DATA] AIRPORT — [Example Data] Arriving Flight 102064 · Arriving Route 87 · Departing Flight 102057 · Departing Route 86 · Runway 4
> — vertex/images/explore_objects_4.jpg

The older one calls the same submenu **Add linked objects**, interleaves
Functions (`fx`) with relations, and — this is the finding — **hides
zero-result relations behind a link**:

> Add linked objects ▸ — INVENTORY ITEM — fx Basic · fx Basic Inventory Item · Alert 1 · BOM Header 1 · MSC Demo - Material 1 · MSC Demo - Plant 1 — Show 4 relations with no linked object results
> — vertex/images/graph_generation_and_search_around-ui.jpg

No sentence in the corpus says a relation with no results is suppressed. That is
a real behavioural rule, visible only there.

### The Search Around panel

> In cases where you want to create a multi-step, filtered graph that crosses different object types, the **Search Around** panel provides a point-and-click interface to build out logic as you generate a new graph.

— `vertex/explore-object-relationships.md`

> The next link in the Search Around will take the resulting object set from the previous link as its starting object set.

— `vertex/explore-object-relationships.md`

> Once you have filtered your object set, you can choose to add this to your graph immediately, or continue building a multi-step Search Around across multiple objects using the **Add link** button.

— `vertex/explore-object-relationships.md`

And the semantics of a downstream filter, which is the subtle bit:

> Once a filter has been applied, the starting object set will be filtered to those objects connected to the filtered resulting object set.

— `vertex/explore-object-relationships.md`

That is a **semi-join back to the start**, not just a filter on the tail. Ours
would have to do the same or the counts would differ.

The panel's own shape, from the image — and note the field is labelled
**Relation**, not "Link":

> Starting objects · [Example Data] Airport 1 ↓ Relation · [Example Data] Departing Flight — Filters · DATE · 1/1/2018 to 1/8/2018 — Resulting Objects · [Example Data] Flights 157 ↓ Relation · [Example Data] Destination Airport — Filters · AIRPORT STATE CODE · NY — Resulting Objects · [Example Data] Airport 1 — Add link · Add to graph
> — vertex/images/explore_objects_9.png

### Parameters — and a third type vocabulary

> To enhance reusability of Search Arounds, you can define parameters which can be used throughout the Search Around.

— `vertex/explore-object-relationships.md`

The type set is enumerated only in the dropdown:

> Add parameter — String · Number · Boolean · Date · Timestamp
> — vertex/images/explore_objects_9.png

**Five types.** Compare the Search Around *function* argument set, which is
prose:

> When used through the toolbar or right click menus, functions may have additional arguments of type `Integer`, `Double`, `Float`, `string`, `boolean`, `Timestamp` or `Date`.

— `vertex/generate-graph-functions.md`

**Seven types**, splitting `Number` three ways. And a third: the graph-template
non-object parameter, which the prose claims mirrors the function set —

> Non-object parameters are additional parameters that can be used as arguments to custom Search Around functions or saved Search Arounds. The supported types for non-object parameters mirror the non-object arguments supported by Search Around functions.

— `vertex/graphs-template.md`

— and whose UI dropdown, in the image, indeed offers `Integer` rather than
`Number`:

> Non-Object Parameters — 123 Max # of flights · Required · Type: Integer · Default Value
> — vertex/images/template-configure-parameters.png

So **three parameter type vocabularies coexist in one product**: the panel's
five, the function signature's seven, and the template's seven-by-reference.
The `Float` in the function list has no rendered field in the generated form —
that form shows `_DOUBLE`, `_INTEGER`, `_DATE`, `_STR`, `_BOOL`, `_TIMESTAMP`:

> Weather Station Parameter — Function Inputs — _DOUBLE · _INTEGER · _DATE · _STR · _BOOL · _TIMESTAMP — Execute Search Around
> — vertex/images/graph_generation_and_search_around-parameters.jpg

I am not concluding `Float` is unsupported; the capture may simply not have used
one. Marked as a question.

### Saved Search Arounds

> If you would like to reuse this Search Around in other graphs or within graph templates, you can save it as a resource.

— `vertex/explore-object-relationships.md`

> Upon loading the Search Around, you will be prompted to select objects from the graph to be used as the starting object set for the search around. Only objects of the appropriate type can be selected.

— `vertex/explore-object-relationships.md`

The tab menu that does this is enumerated only in the image, and one entry is
disabled:

> Search Around 1* ▾ — Rename · Duplicate · Remove (disabled) · Save
> — vertex/images/explore_objects_12.png

The trailing `*` on the tab name is an unsaved-changes marker; no sentence says
so, and I am reading it as one by convention.

### Search Around functions — the one hard contract in the section

> **Search Around** functions are written in a TypeScript functions repository.

— `vertex/generate-graph-functions.md`

> Each function must have exactly one argument that is an Ontology object type or a list of Ontology objects, and the function must have the return type `IGraphSearchAroundResultV1`, as detailed below.

— `vertex/generate-graph-functions.md`

> Vertex will discover the Search Around function using the name and structure of its return type, so it must be declared exactly as follows:

— `vertex/generate-graph-functions.md`

That last sentence is the load-bearing one: **discovery is structural, by the
name and shape of the return interface**, not by a registration. The interface is
printed in full on the page (five optional fields; two edge shapes) and I am not
re-typing it here — the page is the spec.

The four behavioural rules under it:

> `directEdges` will be represented on the graph as an edge directly between two objects. If this edge is based on a link, its `linkTypeRid` as found in the Ontology can be supplied to show the link type's display name along this edge and make Vertex aware of the edge direction.

— `vertex/generate-graph-functions.md`

> Vertex only groups an object into an edge if the function adds that object to the graph, so an object that is already on the graph, including any object you pass as an input to the function, remains its own node.

— `vertex/generate-graph-functions.md`

> `objectRidsToGroup` allows you to group objects into a single node by returning an array of groups, where each group is an array of object RIDs. Note: If you add the same RID to multiple groups, the groups will be merged together.

— `vertex/generate-graph-functions.md`

> `direction` allows you to alter the directionality of the functional link produced by your search around function. The provided value must be one of `NONE`, `FORWARD`, or `REVERSE`. Defaults to `FORWARD` if omitted. Links that appear due to the presence of a `linkTypeRid` are not affected by `direction`.

— `vertex/generate-graph-functions.md`

`NONE | FORWARD | REVERSE` is the cleanest enumerated set in the section, and the
override precedence — a real `linkTypeRid` beats a declared `direction` — is
stated outright.

Publication and permission rules:

> Vertex will use the latest published version of a function. To publish your Function, you need to tag the branch/commit you want with a semantic version, for example `1.0.0`.

— `vertex/generate-graph-functions.md`

> Your repository needs access to all the Ontology objects & links you want to use in your function. This is configurable under the **Ontology** section of the **Repository Settings**.

— `vertex/generate-graph-functions.md`

The screenshot for that last one adds the shape of the grant, and the word it
uses for a link type:

> Ontology Imports — Import object types and relations to use them in Functions. — Object types | Relations — Reload
> — vertex/images/graph_generation_and_search_around-troubleshoot.jpg

**Vertex and Functions call a link type a Relation.** It is the tab name here,
the field label in the Search Around panel, and the menu entry in the template
wizard (§5). Our schema says `link_types`. That is a two-vocabularies pair of
exactly the kind CLAUDE.md tabulates, and it is recorded, not resolved.

### Derived properties

> Functions can be used to create **derived properties** on objects. These can be displayed in the object view, shown in extended node labels and used to color nodes in the layer styling options.

— `vertex/derive-property-functions.md`

Seven criteria are listed on the page. The one that is a design constraint rather
than a type rule:

> This ensures the function is not called for every object on the graph individually, which can be cause very slow performance for large graphs.

— `vertex/derive-property-functions.md`

(The `can be cause` is the page's own typo, quoted as printed.) And:

> The method has no other inputs besides the object set.

— `vertex/derive-property-functions.md`

We already have a `derived-properties.md` reading and a derived-properties
engine. **This page describes a different mechanism with the same name** — an
ad-hoc, per-graph, Function-computed column added from the selection panel's `+`
button, not an ontology-level derived property. Worth checking before either is
extended.

---

## 5. Graph templates

> Vertex graph templates are resources that generate graphs with a defined styling based on parameters.

— `vertex/graphs-template.md`

> Any Vertex analysis can be converted into a graph template.

— `vertex/graphs-template.md`

> Object parameters are object inputs to your template which will be added to the graph when the template is used. Adding object parameters also allows you to perform Search Arounds and functions on the objects provided by the graph template user.

— `vertex/graphs-template.md`

> Each object parameter can be associated with Search Arounds, which can be either simple Search Arounds using Ontology links, Search Around functions, or saved Search Arounds which were built using the Search Around sidebar.

— `vertex/graphs-template.md`

The wizard has **five** numbered steps. The prose accounts for four of them and
a summary:

> Save as Template — 1 Configure parameters · 2 Configure Search Arounds · 3 Configure layers · 4 Configure graph · 5 Configure defaults
> — vertex/images/template-configure-parameters.png

**Step 5, `Configure defaults`, is in no sentence.** The summary screen shows
what it holds, and it is one word:

> Template summary — Parameters ⚠ · Layers · Graph (Name · Description · Layout: Auto) · Defaults — Pinned items — Edit Template · Save Template
> — vertex/images/template-summary.png

`Pinned items` appears nowhere else in the section. I do not know what it pins.

Step 2's menu gives the three Search Around kinds their UI names — and the first
is `Relation`, not "Ontology link" as the prose has it:

> Add Search Around — Relation ▸ · Function ▸ · Saved Search Around ▸
> — vertex/images/template-configure-search-arounds.png

Step 3 splits layers the way the layers panel does, and makes inclusion a
per-layer dropdown rather than the binary the prose implies:

> Node layers — Retain node layers styling or exclude them. — [Example Data] Aircraft · Include as: Styling — Edge layers — Retain edge layers styling or exclude them
> — vertex/images/template-configure-layers.png

An object parameter carries two badges the prose never mentions, one of which
implies a cardinality choice:

> Event to investigate — Required · Single object — Description: Event for which same-day delayed flights will be found — Type: [Example Data] Flight Delay Event · Active
> — vertex/images/template-configure-parameters.png

`Single object` reads as one end of a single/multiple toggle. Marked as
inference; no sentence names the alternative.

Using a template:

> An object or an object set can also be preloaded into a template using URL query parameters:

— `vertex/graphs-template.md`

> To interact with the given parameter values, use the **Parameters** button in the top toolbar. This will allow you to select all the objects used as values for a given object parameter, or change the parameter values to regenerate your graph.

— `vertex/graphs-template.md`

And the templates are shippable:

> All Vertex graph template features are supported with the exception of saved Search Arounds.

— `vertex/marketplace-vertex.md`

That exception is precise and useful: a saved Search Around is a *separate*
resource with its own RID, so it does not travel inside the template. It also
confirms saved Search Arounds are first-class resources, not template-local
config.

---

## 6. Generating a graph from elsewhere — the URL contract

`generate-graph-apps.md` is short and entirely specification. Three parameters
are creation-only:

> `objectSetRid`: Adds all objects in the specified object set as nodes to the graph.

— `vertex/generate-graph-apps.md`

> `searchAroundFnRid`: Adds to the graph the result of the specified **Search Around** function. The function will be called with either a single object if used with the `objectRid` parameter, or all the objects from the `objectSetRid` object set.

— `vertex/generate-graph-apps.md`

Three apply to both new and existing graphs (`selectedTime`, `startTime`,
`endTime`), with a stated defaulting rule:

> All times can be either UNIX timestamps in milliseconds or ISO-formatted dates/datetimes (e.g. `2020-02-15/2020-02-15 13:45:00 UTC`). If `selectedTime` is specified but at least one of `startTime` and `endTime` is not, the time range will have the default duration and be centered around the selected time. If `startTime` and `endTime` are specified but `selectedTime` is not, the selected time will be the same as `startTime`.

— `vertex/generate-graph-apps.md`

The two route shapes are printed as `/workspace/vertex/graph/create` and
`/workspace/vertex/graph/{graphRid}`. **These are the only concrete Vertex
identifiers in the corpus.** See §12 on RIDs.

---

## 7. Events, time series, thresholds and the timeline

> Events are object types configured in the Ontology that include temporal information—minimally, two timestamps that indicate the start and end of the event.

— `vertex/events-overview.md`

> An **event** is defined and configured within the Ontology. All events must have a distinct start and end time and can be configured with specific thresholds to define the color shown for the event.

— `vertex/explore-related-events.md`

An event is therefore **not a new kind of thing** — it is an object type with two
timestamps and a type class. That is the single most reusable fact in this
section for us.

The intent set, given four ways on one page:

> * Orange: `kind`: `vertex`, `name`: `event_intent.warning`
> * Red: `kind`: `vertex`, `name`: `event_intent.danger`
> * Blue: `kind`: `vertex`, `name`: `event_intent.primary`
> * Green: `kind`: `vertex`, `name`: `event_intent.success`

— `vertex/configure-events.md`

Where the type classes are configured:

> You can configure these type classes in the **Capabilities** tab for the event object in the Ontology Manager or directly in the type classes for the properties of the object.

— `vertex/configure-events.md`

The Capabilities panel for that is the one place the field labels appear, and it
carries a field the prose never mentions plus an `Experimental` badge:

> Vertex — configure event behavior in the Vertex modeling and simulation platform. — Event intent · Color of the event · success — Event value · Numeric value of the event · Volume · Experimental — Event value unit · Unit of measurement for numeric value of the event
> — vertex/images/optional_ontology_config-event.jpg

**Thresholds.** `configure-thresholds.md` is a seven-line stub; its single image
carries the entire field set:

> Thresholding — show warnings when a time series measurement breaches expected limits — Threshold Measure · Vertex will indicate when this Measure is outside limits. — Threshold low limit · Lower bound — Threshold high limit · Upper bound — Threshold exceed intent · Color to use, when indicating threshold breaches · warning
> — vertex/images/optional_ontology_config-threshold.jpg

The prose for that page is one sentence:

> Within the Vertex Capabilities in Ontology Manager, you can configure Thresholds on time series measures which will indicate when a threshold has been breached.

— `vertex/configure-thresholds.md`

**Time series** are optional in an installation, which is stated plainly and is
relevant to us:

> Not all customer installations include time series. If you would like to learn more about these capabilities, contact your Palantir representative.

— `vertex/explore-time-series.md`

The per-series menu offers `Open in Quiver / Object Explorer`, `Add to extended
label/readout`, and `Open in series view`. Quiver and Object Explorer we have;
this is a direct hand-off point.

**The timeline** (`timeline.md`) is a separate bottom panel from the series
panel. Two things there are image-only and both are semantics, not chrome. The
time filter's actual predicate:

> Some Date property is between 12/15/1996, 11:59:00 AM and 12/23/1999, 4:07:59 PM or Object has no Date properties — Clear filters
> — vertex/images/timeline_overview-expanded.png

The prose only says matching nodes are opaque and non-matching are faded. The
chip says the predicate is **existentially quantified over date properties and
disjoined with "has none"** — i.e. an object with no date property is *kept*, not
filtered out. That is a default nobody would guess.

And the timeline bar carries a timezone selector:

> Time filter 12/15/1996 – 12/23/1999 · Jul 12 2023 1:53:57 PM • Latest Data · Time range 12/15/1993 – 7/12/2023 · Denver (MDT) -06:00
> — vertex/images/timeline_overview-expanded.png

Timezone is mentioned on no page in the section. Playback adds a `View Latest →`
affordance likewise unmentioned:

> May 09 2008 1:41:30 PM · View Latest → · ▶ · 10x
> — vertex/images/timeline_playback_controls.png

against the prose:

> You can use the play button (⏵) to move the time cursor automatically; playback speed can be adjusted with the speed presets (1x, 2x, 5x, 10x, 100x, and so on).

— `vertex/timeline.md`

Time selection (the top-right panel, distinct from both the timeline and the
series panel) supports comparison:

> You can choose to create multiple time selections for comparison.

— `vertex/use-time-selection.md`

So Vertex has **three** distinct temporal surfaces — time selection, series
panel, timeline — and §9 confirms it by giving each its own Workshop toggle.

---

## 8. What Vertex asks of the Ontology — the `vertex` type-class set

`metadata-typeclasses.md` is the page that LISTS the set, and by CLAUDE.md's
enumeration-beats-description rule it wins over any Vertex page describing a
member. It carries **seventeen** `vertex` rows, one of them marked Deprecated.
Grepping them needs care: the mirror escapes the underscores inside the table, so
`link_merge` is on disk as `link\_merge` and the obvious grep returns nothing.

The link-merge trio:

> For Vertex & Vortex Related Object Search Arounds, always treat this object as an intermediary - this object will no longer show up in Related Object list, but second-degree links from it will, and it will render as an edge on the graph. Place type class on the primary key of the object.

— `object-link-types/metadata-typeclasses.md`

> The same as `link_merge`, but only for this specific relation - the link merged object is the target/to side of this relation.

— `object-link-types/metadata-typeclasses.md`

Note both ends are separately nameable — `link_merge_incoming` and
`link_merge_outgoing` — which is the same two-ends-of-a-link shape as the edge
styling menu header in §3.

The intent and threshold families:

> Set this on the primary key property of an Event to use it in Vertex & Vortex, where `intent` indicates the color/severity of the event/alert (danger, warning, primary, or success).

— `object-link-types/metadata-typeclasses.md`

> For time series objects: Vertex will alert when series values fall below this minimum.

— `object-link-types/metadata-typeclasses.md`

> Measures listed here will display on the Vertex home page.

— `object-link-types/metadata-typeclasses.md`

That last one is a dangling thread: **the Vertex home page is referenced twice
and described nowhere.** Grepping the whole mirror for `vertex home` returns
exactly that one table row.

And one that is dead:

> For time series objects: A JSON map from numeric values to string values. The type class is no longer supported.

— `object-link-types/metadata-typeclasses.md`

### The Capabilities panels, which are wider than the type-class table

Three Ontology Manager captures show the object-type-level Vertex configuration,
and **two of the panels contain controls with no type class in the table and no
sentence anywhere in the corpus.**

The Search Around panel — which is what `link-merging.md` is really about, though
that page calls it Link Merge:

> Search Around — configure specific Search Around behavior for the Vertex and Map applications. — Link merge always · Treat this object type as a Relation — skip over it when link-traversing · Off/On — Incoming links to merge — Outgoing links to merge — Links to ignore cardinality · Links that are ignored for cardinality checks and we assume the relation is many-to-many. — Hidden outgoing links · Prevents outgoing links from appearing in Vertex/Maps search around menus.
> — vertex/images/optional_ontology_config-link_merge.png

I grepped the entire mirror for `ignore cardinalit` and for `hidden outgoing` /
`hide outgoing`: **zero hits in any `.md`.** Those two controls exist only inside
that screenshot. `Links to ignore cardinality` in particular is a
semantics-changing switch — it makes the traversal treat a link as many-to-many
regardless of its declared cardinality — and nothing documents when it is safe.

The prose it is supposed to correspond to is two sentences:

> Configuring **Link Merge** in the Vertex Ontology Capabilities section in the Ontology Manager will allow objects to be automatically grouped into the edge between two nodes.

— `vertex/link-merging.md`

> You can automatically group transactional objects into the related edge in the Ontology Manager → Capabilities tab.

— `vertex/graphs-display-options.md`

The graph-level panel, from the media-layers walkthrough, is wider still:

> Vertex — each Object is the title object for a Vertex graph. — Default Object Selection Panel · Sets the default panel to show in the object selection view — Key Measure · Measures listed here will display on the Vertex home page. — Vertex Promoted Graphs · Graph Rids listed here will display on the Vertex home page along with the associated object — Image reference property · Media Reference · Experimental — Image annotation coordinate property — Modeling and simulation — configure series behavior in the Vertex modeling and simulation platform
> — vertex/images/media-layers-config-ship-diagram.png

**`Vertex Promoted Graphs` appears nowhere in prose and has no type class in the
table.** It is a property holding graph RIDs, surfaced on the undocumented home
page. And `Modeling and simulation` is a *fourth* collapsed Vertex capability
block, unopened in that capture.

`Default Object Selection Panel` is the case where the enumeration lives in
another section entirely. `map/selection.md` prints both the four tabs and, in an
image's alt text, the four dropdown values:

> A section in Ontology Manager labeled "Default Object Selection Panel" and captioned "Sets the default panel to show in the object selection view" with the dropdown options of "Property list", "Series list", "Events list" and "Object view panel"

— `map/selection.md`

The Vertex selection-panel capture I opened shows only **three** tabs —
`Properties`, `Series`, `Events 0` — with no Object view:

> [SFO] San Francisco Internatio… · [Example Data] Airport — Properties · Series · Events 0 — Filter…
> — vertex/images/explore_objects_5.png

Whether Vertex genuinely lacks the fourth tab or the capture predates it, no
sentence says. Marked as a question, because a build would have to choose.

---

## 9. Embedding in Workshop

`embed-graph-workshop.md` is the longest page in the section and the most
directly relevant to us, because Workshop modules are something we have.

> Graphs, graph templates, and diagrams can be embedded in Workshop modules, allowing users to interact with Vertex visualizations directly within Workshop workflows.

— `vertex/embed-graph-workshop.md`

Three embeddable resource kinds — graph, graph template, diagram — and a fourth
identifier form that appears in exactly one clause:

> **Variable:** When this resource mode is selected, the input resource is provided by a string variable which should contain a graph, graph pointer or graph template RID.

— `vertex/embed-graph-workshop.md`

**`graph pointer`** occurs once in the entire mirror, right there. It is a
resource kind nothing defines.

The deprecated input mode — the section's only `legacy` hit that is not about
models:

> Previously, template inputs were supplied using a single input object set variable that contained all of the required object types defined in the graph template. This is now discouraged, as it only works for templates without non-object parameters and one object parameter per object type.

— `vertex/embed-graph-workshop.md`

The widget has five config sections. Two enumerations are prose-and-image
agreeing:

> * **All:** All actions on any object on the graph are available.
> * **Some:** Only the specifically selected actions are available.
> * **None:** No actions are available.

— `vertex/embed-graph-workshop.md`

> AVAILABLE ACTIONS — All | Some | None
> — vertex/images/workshop-interaction-section.png

> SELECTED TIME — Default | Timestamp | Date — TIME WINDOW — Default | Timestamp | Date
> — vertex/images/workshop-time-configuration.png

The **Interface** section is where the image adds most. The prose lists fifteen
toggles; the image gives their **default states**, which the prose never does,
and lists them in a different order:

> LEGEND/OBJECTS PANEL on · TIME SELECTION PANEL on · SERIES PANEL on · ENABLE TIMELINE off · IS TIMELINE OPEN off · ENABLE LAYERS PANEL on · ENABLE SELECTION PANEL on · ENABLE SEARCH PANEL on · ENABLE HISTOGRAM PANEL on · ENABLE INFO PANEL off · ENABLE VERSION HISTORY PANEL off · ENABLE ADD OBJECT on · IS LEGEND/OBJECTS PANEL COLLAPSED on · ENABLE SUBGRAPH NAVIGATION MENU off · INCOMPLETE INPUTS MESSAGE
> — vertex/images/workshop-interface-section.png

The **Capabilities** section image contradicts the prose's either/or reading of
one control. The prose presents `On create new graph` as a choice between Event
and Action; the image shows Action selected **and an event field still present
beneath it**:

> ON CREATE NEW GRAPH — Action | Event — ACTION on success · Select an action… — EVENT on action submit · Add event — SHOW SAVE DIALOG — None | Resource dialog — SAVE AS VERSIONED GRAPH off
> — vertex/images/workshop-capabilities-section.png

And the **Input** section carries a button in no sentence:

> INPUT — RESOURCE — Static | Variable — Select — Create new template — REGENERATE GRAPH AFTER APPLYING SCENARIO
> — vertex/images/workshop-input-section.png

Creating a Vertex template from inside the Workshop widget editor is a real
authoring path the documentation does not mention.

The **save-and-share pattern** at the end of the page is the most concrete
integration recipe in the section, and its first step is a schema constraint:

> This property must be a string with **Value Formatting** using the **Resource RID** option.

— `vertex/embed-graph-workshop.md`

> Following this pattern, when a user selects the save icon, the current graph state will be saved as a resource, and the property on the object will be updated with the RID of the saved graph.

— `vertex/embed-graph-workshop.md`

Two rules constrain what an embedded graph may do, from `graphs-explore.md`:

> Changes made to a graph within a Workshop module will not update the underlying template used to generate the graph. If you wish to permanently change a view within Workshop, you will need to update the underlying template.

— `vertex/graphs-explore.md`

> Changes made to styling within a Workshop module are not persisted. You can, however, configure multiple *saved styles* within the underlying template.

— `vertex/graphs-explore.md`

Read-only mode is the other constraint, and its trigger list is explicitly
partial:

> * New objects cannot be added to the graph (including via Search Around).
> * Graph nodes cannot be re-arranged (whether by drag-and-drop or other methods).
> * The toolbar at the top of the page is hidden.

— `vertex/read-only-mode.md`

> Below is a non-exhaustive list of the situations where a graph is opened in read-only mode.

— `vertex/read-only-mode.md`

The three listed are Workshop-with-the-toggle, Carbon, and Notepad.

---

## 10. Scenarios and simulation

Read after §1, because most of this page describes the sunset path.

> Scenarios allow you to interact with your modeled universe, unlocking the ability to ask “What if” questions to simulate different operating conditions.

— `vertex/scenarios-overview.md`

> Configured against your system graph, a scenario evaluates actions along with one or more modeled inputs and computes the output value to reflect the real-world interactions of your digital twin.

— `vertex/scenarios-overview.md`

> Consider models as jobs that take a pre-defined set of inputs and return a set of calculated outputs. The model version specifies the input and output parameters and can be configured alongside the ontological objects seen in your system graph.

— `vertex/scenarios-overview.md`

> Once published as actions, Functions on models will be available for configuration in Vertex, where you can interactively run scenarios to understand the impact of potential operating conditions.

— `vertex/scenarios-overview.md`

The non-sunset half of getting started is short — add a scenario, add Actions:

> To begin adding Actions, first select the **Add scenario** button. This will create a new scenario to which you can add Actions.

— `vertex/scenarios-getting-started.md`

The containment hierarchy is only legible in the image, and it is **three
levels**, not two:

> Simulation — Case Study 1 ▾ + — Time · 3/22/2022 09:57:23 AM — Advanced options — Actions — ▾ Scenario 1 · 0 actions applied · Run · … — ⊕ Add action — ⊕ Add scenario — Create simulations to analyze and compare how your system might look under different conditions — To begin, add a model: ⊕ Add new model
> — vertex/images/simulate-system-12.jpg

So: **Case Study → Scenario → Actions (and models)**, with `Run` scoped to the
scenario and a per-scenario applied-actions count. The prose mentions renaming
"this" at the top of the scenario pane and separately renaming individual
scenarios, which only makes sense once you have seen the nesting.

Scenario options are four, from `scenarios-options.md`:

> You can specify the time window for running your scenario. You should select a time where there is known data for the objects in scope of the scenario.

— `vertex/scenarios-options.md`

> For object-based System Graphs, you can choose to set the scope of the scenario to objects shown only in the graph to limit available input/output parameters.

— `vertex/scenarios-options.md`

> This baseline scenario will run the models you have chosen without any actions or overrides, providing you with a baseline against which to compare your other scenarios and better judge the impacts of your actions.

— `vertex/scenarios-options.md`

plus time-series smoothing in minutes. Parameters come in three flavours:

> From here, you can choose to add individual time series, object properties, or measures to your scenario.

— `vertex/scenarios-options.md`

And overriding is an in-table edit:

> Any parameters chosen will be shown within the scenario table. If the parameter is an input, you can override it by manually editing the value within the scenario table prior to running a scenario.

— `vertex/scenarios-options.md`

Chained models (sunset) propagate outputs to inputs by parameter mapping:

> Any value produced for a model output that is mapped to a parameter and used as input to a later model will propagate as the input value to that model. This value is denoted in the input cell using a link icon.

— `vertex/chained-models.md`

**That propagation rule is the interesting part and it is the part being
retired.** Under the recommended replacement — functions imported into
function-backed Actions — nothing in the corpus says how output-to-input chaining
is expressed. Recorded as the top question.

---

## 11. Media layers, and saving

`media-layers.md` is a single worked example, notionally a WWII ship's deck plan.
Its requirements:

> To get started using media in Vertex, ensure you have at least one object type with a media reference.

— `vertex/media-layers.md`

> From the **Capabilities** tab, choose which image reference to use as the default image in a media layer.

— `vertex/media-layers.md`

> You can configure which of the object type properties is used by default as the pixel space bounding box coordinates of your image annotations.

— `vertex/media-layers.md`

The annotation *objects* are ordinary object types linked to the media object
type by a foreign key, and creating an annotation runs an **Action**:

> If you configured action types to create annotations for objects linked to the object type of the media, you will see the options to run those actions in a dropdown menu.

— `vertex/media-layers.md`

> Notice that the coordinates of the annotation will be used as the bounding box coordinates for the new object, which will vary depending on how you configured the action for your object type.

— `vertex/media-layers.md`

> When you move or resize an annotation, any action types that can modify the object type will appear below the annotation.

— `vertex/media-layers.md`

So a drawn rectangle is an **Action parameter value**, and annotation persistence
is Action persistence. Nothing new is stored. That is a genuinely elegant design
and it is directly buildable on what we have.

The add-object dialog enumerates its two modes in prose and image:

> **Object** is the default option and will add the objects to the graph with their configured Ontology Manager object type icon and color. Choose the **Object with media** option to add and view annotations on the object.

— `vertex/media-layers.md`

> Object ✓ · Object with media — Add all (7) · ⊕ Add selected
> — vertex/images/media-layers-filter-object.png

The search dialog adds a hard precondition the prose does not state — you cannot
search before choosing a type — and gives the scope filter its exact labels:

> Object types — Filter… · All objects ✓ | Objects with media — Results (125) — Filters — Select an object type · This search requires selecting object type(s) first. — Object ▾ · Add all · ⊕ Add selected
> — vertex/images/media-layers-search-dialog.png

(The object-type names in that capture are blurred; I am reporting the structure,
not the list.)

**Saving and sharing.** The permission behaviour is stated precisely and is the
most important sentence on the page:

> Sharing a graph will not grant users access to any other resources to which a user does not already have access. When opening a graph, if a user does not have access to any objects or time series referenced in the graph due to permissions or deleted data, they will still see the structure and shape of the graph. The user will not see the specific data pertaining to the objects.

— `vertex/save-share.md`

**A graph leaks its shape but not its data.** That is a deliberate, documented
compromise and it is exactly the kind of thing a naive RLS implementation gets
wrong in one direction or the other.

Versioning is opt-in and revert is non-destructive:

> If you would like to keep track of changes to your graph over time, you can select **Enable versioning** from the dropdown **Save** menu. Subsequent saves will create a new version of the graph.

— `vertex/save-share.md`

> By selecting **Revert** from the Graph History sidebar, a new version will be created with the same contents of the selected version.

— `vertex/save-share.md`

The share panel's role list is image-only here:

> Link sharing on — Viewer ▾ — Discoverer · Can only see files' name and metadata — Editor · Can edit files and modify sharing. — None · Default for Regalia, in order to maintain Training Access Controls. — Owner · Can edit files and has full control over their security. — Viewer · Can view the contents of files, but can't edit them and can't manage their security
> — vertex/images/save_share_collaborate-draft.jpg

That capture shows five entries, but the enumerating source says four, and it is
an `api/` page — the kind CLAUDE.md says wins:

> The default roles in Foundry are: Owner, Editor, Viewer, and Discoverer.

— `api/filesystem-v2-resources-resource-roles-list-resource-roles.md`

The fifth, `None`, describes itself as an enrollment-specific default, so the
screenshot and the API agree: **four default roles plus whatever an enrollment
adds.** Worth flagging against our own notes, which record that no `Editor`
*space role* exists — a different granularity, and not a contradiction, but the
two are easy to conflate.

The same capture also shows the save dialog's filesystem roots — `Data Catalog`,
`All projects`, `Recent files`, `Favorites`, `Shared with you`, `Your files` —
which is Compass, not Vertex, and matches what we already have.

Control Panel, finally, is where the limits live:

> To modify Vertex settings, you will need the `Vertex Admin` role.

— `vertex/vertex-settings-control-panel.md`

> **Maximum number of ungrouped objects:** The maximum number of objects resulting from a search around that Vertex will add to the graph as individual nodes, rather than groups. If more objects than this limit would be added to the graph, the objects will be automatically grouped.

— `vertex/vertex-settings-control-panel.md`

> **Template search around max depth:** The maximum number of nested Search Arounds allowed when creating a template.

— `vertex/vertex-settings-control-panel.md`

The Control Panel screenshot shows **four** tabs where the prose has three
headings, and the extra one is not a rename:

> Vertex — Manage defaults for new graphs, data loading limits, and graph publishing configuration for Vertex. — New graph defaults · Data loading · Graph publishing · Model configuration
> — vertex/images/control-panel-vertex.png

**`Graph publishing` is a whole tab with no prose at all.** The page's own
subtitle names it. The same capture supplies a default the prose omits and a
mechanism it never mentions:

> Default time selection — The number of days that the default time selection spans. — 10801 — (default: 180) — Default graph mode — Organization default — Graph | Diagram — Group overrides — For a Vertex user, the first group in the list below that they are a member of will take precedence and determine their default graph mode. If a user is not in any of these groups, the organization default will be used. — Add group override
> — vertex/images/control-panel-vertex.png

Per-group overrides with **first-match-wins precedence** is a real configuration
mechanism, and it is nowhere in any sentence.

---

## 12. What I checked across the corpus

**RIDs: none attested.** I grepped `vertex/` for `ri.` and got zero hits, then
grepped the whole mirror for `ri.vertex` and got zero. The section names five RID
*variables* — `objectRid`, `objectSetRid`, `searchAroundFnRid`, `graphRid`,
`linkTypeRid` — and prints no literal RID for any of them. The only concrete
identifiers are the two URL paths in §6. Our `rid-grammar.md` reading cannot be
extended to Vertex resources from this section; there is nothing to extend it
with.

**Contradictions and near-misses found by grep:**

1. **Two layout enumerations** (§3) — ten UI entries, six wire values, disjoint
   at both ends. Neither page claims to be the complete set.
2. **Three parameter type vocabularies** (§4) — five / seven / seven-by-reference.
3. **`Relation` vs link type** (§4, §5, §8) — Vertex, Functions ontology imports
   and the template wizard all say Relation; the ontology pages say link type.
   `link_merge_incoming` splits it by end, matching our link-type-view work.
4. **Two names for one submenu** — `Search Around` in the newer capture,
   `Add linked objects` in the older, over the same content.
5. **`Object view` selection tab** — documented for Map in `map/selection.md`,
   absent from the Vertex capture I opened.
6. **`link-merging.md` calls the panel Link Merge; the panel calls itself Search
   Around** and holds five controls, two of which exist nowhere in prose.
7. Controls with zero prose coverage anywhere in the mirror, all image-only:
   `Links to ignore cardinality`, `Hidden outgoing links`, `Vertex Promoted
   Graphs`, `Graph publishing` (Control Panel tab), `Configure defaults` /
   `Pinned items` (template step 5), `Create new template` (Workshop input),
   the Vertex **home page** itself, and `graph pointer` (one clause, no
   definition).

None of these is a legacy-model artefact of the kind CLAUDE.md predicts; they are
straightforwardly under-documented surfaces.

---

## 13. What this connects to in what we already have

- **The ontology.** Object types, link types, per-type indexed tables and object
  sets are all Vertex needs to read. An *event* is an object type with two
  timestamps plus a type class (§7) — no new entity. This is the cheapest real
  connection in the section.
- **Type classes / Capabilities.** We have `capabilities-typeclasses-and-branching.md`
  and a shipped Capabilities tab. The `vertex` kind is seventeen more rows in the
  same table, and §8 shows the Capabilities UI renders them as labelled fields
  with descriptions — which is what our tab already does.
- **Quiver (696–698).** `explore-time-series.md` hands a series off to Quiver by
  name. Both read the ontology and store nothing of their own; the `timeseries`
  type-class family in the same table is what Quiver discovers by.
- **Models and modeling objectives (699–702).** This is the live seam. Vertex's
  own model configuration is *sunset* and points at Modeling objectives — which
  we have — and then at function-backed Actions, which we have as Actions.
- **Actions.** The non-sunset scenario path is entirely Actions; media
  annotations are entirely Actions (§11). Vertex needs no write path of its own.
- **Workshop modules.** §9 is a widget contract against variables and events we
  already model.
- **Compass / roles.** Graphs and templates are ordinary Compass resources with
  ordinary roles and link sharing (§11).
- **Object Explorer / object sets.** Search Around is object-set traversal with
  filters and a semi-join back to the start (§4); `object_set_where` is the
  nearest thing we have.
- **Derived properties.** Name collision with our shipped engine, different
  mechanism (§4). Check before extending either.

---

## 14. Where Vertex needs a runtime we do not have

Recorded as divergences, not as things to invent around.

1. **A graph rendering and layout engine.** Ten layouts with per-layout
   parameters (root selection, staggering, density 1–5, cartesian normalisation,
   clustering by property). This is a client-side graph library. Postgres has no
   part in it and neither does our function isolate.
2. **TypeScript Functions in a repository, discovered by return-type shape.**
   Vertex finds a Search Around function by the *name and structure of its return
   interface* (§4) and uses the latest semver-tagged publication. We have a
   QuickJS/WASM isolate and versioned functions; we do not have structural
   discovery over a repository, and nothing in our stack reads a TypeScript type
   at runtime.
3. **Time series as a first-class data system.** The section leans on measures,
   interpolation, smoothing windows, polling intervals and staleness warnings.
   The docs themselves say installations may not have it (§7).
4. **Models as jobs with input/output parameters, chained.** §10. Even Foundry is
   retiring the Vertex-side half.
5. **Media sets and media references with pixel-space coordinates.** §11. We have
   `media-reference-properties.md` as a reading; the annotation *workflow* is
   Actions, which we could do, but the media substrate is not ours.
6. **Diagram mode**, beta and controlled by an icon-category allowlist in Control
   Panel (§11) — an entire second visual product this section barely describes.

The honest summary is that **the parts of Vertex that are ontology reads —
Search Around traversal, layers, event/threshold type classes, saved graphs as
Compass resources, Action-driven edits — are buildable on what we have. The parts
that make it Vertex are a graph renderer and a time-series stack.**

---

## 15. Corrected BEFORE building — the adversary pass

A foundry-adversary pass ran before any migration was written. The image
accounting held exactly (the first reading where that claim came back fully
mechanical and fully true), every sampled quotation was byte-exact, and the
semi-join inference was confirmed — and the pass falsified the reading's
largest structural claims:

**A scenario IS a documented resource, and it is an ONTOLOGY primitive.**
My claim that Vertex has no store and that no scenario identifier is
published anywhere was wrong by twelve mirrored pages and twenty-three api files carrying `scenarioRid`:

> A scenario is essentially a sandbox to apply edits on top of the data in your Ontology, generated by applying one or more actions.

— `ontology/overview-ontology-scenario.md`

> If the function reads from the Ontology, it will read the state of the Ontology on the scenario. If the function makes Ontology edits, it will make those edits on the scenario.

— `ontology/overview-ontology-scenario.md`

— which answers my own top question (how the non-sunset path chains: the
scenario itself is the chain), plus a 30-day TTL, ten-minute auto-rebase, a
merge action, and a persisted-scenario interface with a Scenario Reference
value type. The corpus ties it to Vertex explicitly — platform-overview
links "the Scenario primitive" to vertex/scenarios-overview, and the
Workshop embed carries a prose "Load data from scenario" toggle I had only
read off an image. §13's conclusion that Vertex needs no write path of its own was wrong:
actions in a scenario write TO THE SCENARIO, never to main. An Action-in-scenario is (action type, parameter values), from the
sentence I skipped mid-page:

> After choosing which Action to add, you must update the parameters of the Action and select **Submit** to save this Action within your scenario.

— `vertex/scenarios-getting-started.md`

**A saved graph contains SUB-GRAPHS.** The word appears once in my
transcriptions and I never followed it; the Workshop embed defines it in
prose ("When embedding an existing graph or diagram resource with multiple
sub-graphs, you can choose which sub-graph to display…"), gives events for
it (Objects on subgraph / Add objects to subgraph), and
questions-answers/vertex — a 29th Vertex page outside the section I never
opened — rules that a graph cannot be copied as-is into a subgraph.
Assembled from the pages: a graph stores specific objects (not a recipe),
node positions, layers, styles, saved selections (≥1 per object,
first-three-colours border rule), groups, sub-graphs and versions — and
the graph-history capture shows a BRANCH chip beside the version chip,
which no prose explains (recorded, not built).

**The event contract is six `timeseries` type classes, not the vertex
set**: event_id, event_start_time, event_end_time, event_description,
event_root_object_id, event_linked_series_id — every one marked "Configure
in **Capabilities** page of object type" on metadata-typeclasses, the page
I had grepped for `vertex` alone. And there are at least TWENTY vertex
type classes, because graphs-display-options defines three more, on link
types, with a default keyed to cardinality:

> This can be configured per link type using the following Ontology type classes:

— `vertex/graphs-display-options.md`

(`link_primary_direction`, `link_undirectional`, `link_bidirectional`.)

**api/ publishes the Search Around wire contract** — `searchAround` is a
first-class object-set union member taking an object set and ONE link
type's API name, "not a filter": the traversal operator in Foundry's own
object-set grammar. My §13 pointer at `object_set_where` was the
citation-that-stops-the-next-look failure; the nearest thing we have is
the ontology package's searchAround, and the SQL side needs the operator,
not the filter.

**Counting corrections:** four creation-only URL parameters
(`selectObjectRid` missed, and it "Must be used in conjunction with either
the `objectSetRid` or the `objectRid` URL parameter"); eight
derived-property criteria, not seven; at least thirteen RID variables, not
five; eight lifecycle-grep lines, not six; the version-history rail IS
prose ("A full version history can be viewed in the **Graph History**
sidebar" — `vertex/save-share.md`); the timeline styling menu's identity
is prose too. Also from the pass: a Search Around parameter stores name,
description and default value; the Workshop embed's Input/Interaction
bullets I skipped include `Append on parameter change` (evidence the graph
is materialised); link-merge's user affordance is the Search Around menu's
"link via"; intermediate edges between the same two objects merge onto
one; and the two OMA Capabilities captures are from two UI eras, undated.

## 16. Post-build reconciliation — what 710–714 built, against the corpus whole

Done 2026-08-27 after #880 merged and Deploy verify went green: a foundry-gap
pass over all 27 vertex pages plus the five ontology scenario pages, diffed
against the applied schema in both directions, its top findings then
re-verified by me against the pages and the live catalogues. The verdict
first, then the findings in consequence order.

**The graph and the template are Foundry's shape; the scenario side is not,
three ways, all pointing the same direction.** A graph as materialized
objects with positions in sub-graphs, edges carrying their link type, layers
per (sub-graph, type), versions on save, a project resource with a RID — that
holds against `save-share`, `embed-graph-workshop` and `generate-graph-apps`
read together. The template — object parameters carrying Search Arounds of
three kinds, value parameters over the function argument set — holds too,
with the capture-derived parts declared (714 corrected the one that
over-claimed). The scenario divergences are structural, not omissions, and
none of them is recorded in §10–§15 because all three are consequences of
build decisions taken after the corrections were folded.

### 16.1 The three scenario divergences

**Merge is a function here; Foundry's merge is an action type.**

> Scenarios are merged through action types that provide granular control
> over the scenario edits and enforce fine-grained execution permissions.

— `ontology/merge-scenario.md`

That page's merge action declares a scope (object types + link types on its
Scenario tab) and carries its own submission criteria. Ours is
`merge_scenario(p_scenario uuid)`, INVOKER, whose only refusal is
`merged_at IS NOT NULL` — under the org-scoped RLS, any org member can merge
any scenario. No `apply_scenario` rule kind exists in `action_rule_kinds()`
(13 kinds, none of them this), no merge scope, no scenario parameter type on
`action_type_parameters`. An enumeration tension to hold rather than
resolve: `action-types/rules.md` lists twelve ontology rules and Apply
Scenario is not among them, while `merge-scenario.md` and
`osdk-scenario.md` both describe it as a rule — and the api's `LogicRule`
union carries `applyScenario` as its ninth member.

**Foundry threads a scenario through the existing read/write engines; we
forked two bespoke entry points.** `scenarioRid` is an optional request
field on eight distinct api endpoints — apply-action, both object-set load
shapes, aggregate, edits-history, execute-query, execute-ontology-sql-query
among them:

> The resource identifier of an ontology scenario to apply the action
> against.

— `api/ontologies-v2-resources-actions-apply-action.md`

Ours: none of `apply_action`, `evaluate_object_set`, `aggregate_object_set`,
`object_state` takes a scenario; instead `apply_action_in_scenario` writes
and `scenario_object_state` reads one primary key at a time. **A scenario
cannot be queried, filtered, aggregated or paged.** This is the exact shape
question `api/` exists to settle, and I did not ask it before building 713.

**A persisted scenario is an object type; ours is a platform table.**

> Temporary scenarios are created and used only within the current session.

— `ontology/temporary-scenario.md`

> You can define and store metadata of a scenario in the Ontology, where
> each object represents a scenario, to create a **persisted** scenario.

— `ontology/persisted-scenario.md`

Foundry has two kinds — not stored at all, or stored as an object type
implementing the Ontology Scenario interface with a Scenario Reference
value-typed key. `ontology_scenarios` is neither: a bespoke always-persisted
platform table, the three-times-mistake shape in a new place (a platform
table where the page says the storage is an object type).

### 16.2 The correctness findings inside the sandbox

- **The preview and the merge replay edits by different rules.**
  `scenario_object_state` folds `scenario_edits` with its own
  create/modify/delete arms; `object_state` — what `index_object_type`
  replays after a merge — nulls every unnamed declared property on create,
  treats delete as terminal, and folds unmerged base `object_edits` in. So
  the state a scenario previews and the state its merge produces can
  disagree (a one-property scenario `create` previews as a one-key object
  and indexes as a fully-nulled row). And my comment on
  `scenario_object_state` says the base is read LIVE — it reads the
  **index**, so base edits since the last build are invisible to a scenario.
- **The redirect drops the two revert columns.** `redirect_edit_to_scenario`
  copies six fields and not `application_id` or `before`; merged rows carry
  NULL and `'{}'`, so `revert_action` (682) cannot revert a merged scenario,
  and `apply_action`'s `action_applications` row is orphaned meanwhile. My
  comment calling `scenario_edits` "object_edits' own shape" is false — the
  two columns it lacks are exactly the revert columns.
- **A scenario cannot hold link edits** — `merge-scenario.md` includes
  "adding or removing links" in the sandbox and `scenario_edits` has no link
  column. Partially a consequence of `create_link`/`delete_link` being
  `executable=false` (no link instance store), but that dependency is
  recorded nowhere on the table.
- **713's `DO $$` never calls `apply_action_in_scenario`** — it sets both
  GUCs by hand and inserts directly. The trigger is proven; the one function
  that is the trigger's caller is proven only by an interactive probe
  nothing re-runs. The assertion rule, violated again by me.
- **The redirect trigger has no `WHEN` clause**, so every `object_edits`
  insert — the hottest ontology write path — calls `current_setting()` for a
  feature with zero rows. 619's lesson in trigger form. And BEFORE triggers
  fire alphabetically, so `guard_object_edit` validates a scenario edit
  against BASE state: a scenario permits modify-after-scenario-delete and
  refuses modify-after-base-delete. 713 declares the first half of this,
  not the second.

### 16.3 Type classes: 710 built a store two of our own maps refuse

`docs/ONTOLOGY-BUILD-MAP.md` §B6 says type classes are **deliberately not
built** ("do not build the bag") and `DELIVERABLE-MAP.md` recorded the same.
710 built the bag anyway, and the enumerating page's own migration note
says the mechanism is being retired:

> The configuration of all supported type classes will move to the
> **Capabilities** page.

— `object-link-types/metadata-typeclasses.md`

The destination already exists here: `object_type_capabilities` +
`capability_slots()` (415), with a shipped Capabilities tab writing it —
and `capability_slots()` already holds all six event slots
(`event_start_time`, `event_end_time`, `event_id`, `event_description`,
`event_root_object_id`, `event_linked_series_id`; verified live). So
`vertex_event_types()` reads a second store: an event type configured
through the shipped tab is invisible to Vertex, and one configured through
710's table is invisible to the tab. Two sources of truth, both currently
empty. Also: the catalogue asserts 33 rows against a page carrying 17
vertex + 17 timeseries rows — `parent` (a Relation),
`timeseries_is_deprecated`, `timeseries_is_value_inverted` and
`timeseries_depth_units` are absent and refuse by name — and the page's
Deprecated column has three states where our boolean has two, the collapsed
third — the configure-in-Capabilities marker — sitting on exactly the rows
the other store models. **Open decision, for a human:** fold the type-class families
into `capability_slots()` and repoint `vertex_event_types()`, or keep the
bag and record why. The real design question either way: `capability_slots`
is object-type-shaped and the three link-direction classes need a link-type
home.

### 16.4 The residuals, now actually recorded

711's comment on `vertex_graph_layers` said groups and saved selections were
recorded residuals in this reading, when the reading contained no such list
— a citation-shaped claim I wrote pointing at a document that did not say
it. This section is now that list.

**Unbuilt, buildable now (no missing runtime):** the ten-layout vocabulary
and its six wire values (neither set exists; `layout text DEFAULT 'Auto'` is
never read); grouping, group-on-edge, saved styles, saved selections;
versioning as opt-in, Duplicate, Revert, and any reader of
`vertex_graph_versions`; link sharing and per-graph roles; the seven-param
URL contract and the two route shapes; the Vertex Control Panel settings
page (including the search-around limits that guard the engine this arc
feeds); histogram filtering (`histogram_object_set` exists, nothing in
Vertex reaches it); Save as Template — templates have a read path and no
write path, the mirror image of the search-around residual; every consumer
of the events convention (badges, Events tab, Open linked events,
`event_intent` colours); **the timeline page in its entirety** — "the
timeline will show lines for an object's time properties and bars for time
ranges in an object's properties" (`vertex/timeline.md`) draws timestamp
properties, not series, so it is NOT time-series-blocked; the scenario
scope-to-graph option; batch-action and nesting refusals on scenarios;
scenario-on-a-branch (we have branches); edited-entity listing; the
execution-context submission criterion ("within a scenario" vs "against the
main Ontology") — the safeguard that makes sandboxes safe to delegate.

**Blocked on a time series store:** `explore-time-series.md` and
`configure-thresholds.md` wholly; `use-time-selection.md`'s scrub and
Compare; `scenarios-getting-started.md`'s model-run panel and
`scenarios-options.md`'s smoothing; the Control Panel's data-loading
settings; the Color-by-measure and series-readout clauses of
display-options.

**Unrecorded until now, unbuilt by choice pending demand:** Marketplace
packaging of graphs/templates, and the Workshop embed widget (§9 specifies
it; nothing implements it — `workshop_widgets` has no Vertex kind). Media
layers were named in §14 but overstated: `object_type_media_sources` (582)
exists, so the missing piece is the two OMA Capabilities fields (default
image reference, pixel-space bounding box), which belong in
`capability_slots()` — the store 710 bypassed.

**Seven of the arc's sixteen tables have no reader**: `vertex_graph_layers`,
`vertex_graph_versions`, `vertex_search_arounds`,
`vertex_template_search_arounds`, `vertex_template_value_parameters`,
`scenario_edits`, `ontology_type_classes`. By CLAUDE.md's rule those seven
are not built yet — they are storage waiting for their surfaces, and this
line is the record that says so.

### 16.5 Two smaller structural notes

`vertex_graphs.read_only` models as resource state what `read-only-mode.md`
attributes to how a graph is opened (Workshop toggle, Carbon, Notepad —
none of which exist here), and the surface honours it only below the fold:
the footer hides, the topbar's Save does not. The search-around steps
grammar (`[{link_type_id, filters}]`) is a second traversal representation
beside `object_sets.traversals` (`[{edgeType, direction, filters}]`, 3-hop
cap, validated) — two grammars for one Foundry concept, neither aware of the
other, unification unrecorded until now.

## Decisions I had to make

1. **I treated the product as not sunset and two of its features as sunset,
   rather than reporting a single verdict.** The task asked for an explicit
   answer either way; the honest answer is split, and collapsing it in either
   direction would be wrong. Quote, not inference: the banners are on
   `chained-models.md` and the `Select models` heading of
   `scenarios-getting-started.md`, and there is no product-level banner.

2. **I did not pick a winner between the ten-entry UI layout list and the
   six-value wire list.** Enumeration-beats-description does not help: both are
   enumerations, of different things (a menu and a JSON field). A build should
   hold **both** sets and map between them explicitly, and should not assume
   `auto-grid` is reachable from the UI or that `Radial` is reachable from a
   function. Inference; the pages never relate them.

3. **I recorded `Relation` as Vertex's word for a link type without renaming
   anything.** Three separate Vertex/Functions surfaces say Relation; the
   ontology pages say link type. By CLAUDE.md's own two-vocabularies rule these
   are one concept addressed to two audiences, and our schema serves the ontology
   audience, so `link_types` stays. Quote for the vocabulary; inference for the
   conclusion.

4. **I treated an event as an object type with two timestamps and a type class,
   not as a new entity kind.** `events-overview.md` and `explore-related-events.md`
   both say so outright. This is the decision most likely to be acted on, so it
   is worth being clear it is quote, not inference — and that it means a faithful
   build adds **no** events table.

5. **I attributed the three-level Case Study → Scenario → Action nesting to the
   screenshot, not the prose.** The prose is compatible with two levels. A build
   that reads only the prose would flatten it. Inference from
   `simulate-system-12.jpg`, marked as such in §10.

6. **I did not propose a shape for a Vertex graph resource, because Foundry does
   not attest one.** No RID, no schema, no API page. Rather than invent
   `ri.vertex.main.graph`, I recorded the absence in §12. **Foundry publishes no
   such identifier anywhere in the mirror; I proposed nothing because any
   proposal would be indistinguishable from a fabricated citation later.**

7. **I opened 36 of the section's 150 captures and chose them by expected
   structural yield** —
   configuration panels, wizards, dropdowns, toolbars — and skipped the
   walkthrough sequences (`simulate-system-*`, `explore-events-*`,
   `media-layers-*` steps) whose outcomes the prose already states step by step.
   That is a judgement call and it is mine; the 114 are named in the header so a
   later reader can check my choice rather than inherit it silently. Three of the
   skipped ones I would open first if this is built: `media-layers-oma-annotation-action-types.png`,
   `optional_ontology_config-link_merge2.jpg`, and `explore_objects_14.jpg`.

8. **I read the `Object view` tab's absence from the Vertex selection panel as
   unresolved rather than as a fact about Vertex.** `map/selection.md` documents
   four tabs and the dropdown that defaults them; the Vertex capture shows three.
   Picking either would be resolving an ambiguity by plausibility.

9. **I cited `map/selection.md`'s image alt text as the enumerating source for
   `Default Object Selection Panel`.** Alt text is prose in the mirrored markdown
   and greppable, so it is citable — but it is weaker evidence than body text and
   I have said so at the point of use rather than quietly.

---

## Questions I could not answer

1. **Under the recommended non-sunset path, how does one model's output become
   another's input?** `chained-models.md` describes propagation by parameter
   mapping and is being retired; nothing says how function-backed Actions chain.
   Searched: all 28 vertex pages, `action-types/function-actions-getting-started`
   by name in `MAP.md`, and grepped the mirror for `chain` near `model`.
   `blocks: any scenario/simulation phase`

2. **What is the Vertex home page?** Two capability fields feed it (`Key Measure`,
   `Vertex Promoted Graphs`) and no page describes it. Grepped the whole mirror
   for `vertex home` — one hit, the type-class table row.
   `blocks: any Vertex navigation phase`

3. **What does `Graph publishing` configure?** A named Control Panel tab, in the
   page's own subtitle, with zero prose. It plausibly relates to promoted graphs
   (Q2), which would make two undocumented surfaces one feature. Grepped for
   `graph publishing`.
   `blocks: nothing` (nothing downstream depends on it yet)

4. **What is a `graph pointer`?** One clause in `embed-graph-workshop.md`, listed
   beside graph and graph template as a third thing a RID can point at. Grepped
   the whole mirror: one occurrence.
   `blocks: any Workshop embed phase`

5. **What do `Links to ignore cardinality` and `Hidden outgoing links` do to
   traversal?** Image-only, and the first changes join semantics — it declares a
   link many-to-many for traversal regardless of its real cardinality. Grepped
   `ignore cardinalit`, `hidden outgoing`, `hide outgoing` across all `.md`: zero.
   `blocks: any Search Around phase`

6. **Is `Float` actually accepted as a Search Around function argument?** Prose
   lists it; the generated form in the capture shows six fields and no float.
   `blocks: nothing`

7. **Does an object parameter have a multiplicity setting?** The wizard badges one
   as `Single object`, implying an alternative that is never named.
   `blocks: any template phase`

8. **What are `Pinned items` under a template's `Configure defaults`?** Step 5 of
   the wizard, one label on the summary screen, no prose. Grepped `pinned` across
   `vertex/`: only that image is the source, and the word appears in the mirror
   elsewhere only for Object View and sidebar pinning.
   `blocks: any template phase`

9. **Does Vertex's selection panel have an `Object view` tab?** See Decision 8.
   `blocks: nothing`

10. **What is the RID form of a Vertex graph, template, or saved Search Around?**
    None attested (§12). Searched `vertex/` for `ri.`, the mirror for `ri.vertex`,
    and `MAP.md` for a Vertex API section — there is none.
    `blocks: nothing yet, but any Vertex resource table later`

11. **Which era is `explore-existing-6.jpg`?** It shows four left-rail tabs where
    the current app shows five. Under the two-Ontology-Managers rule I should
    date a capture before measuring it, and I could not date this one.
    `blocks: nothing`
