{
  "name": "@beacon/ontology",
  "version": "0.0.1",
  "description": "Core ontology and four-layer engine for the Reality Graph",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "lint": "echo '@beacon/ontology - no lint configured'",
    "test": "vitest run",
    "test:watch": "vitest",
    "eval:live": "vitest run src/evals/live/liveJudgment.eval.ts"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^4.1.7"
  }
}
