// Shared hooks will be added here as features are built.
// Pattern: feature-specific hooks that wrap TanStack Query live in
// packages/hooks so they can be reused by web and (future) mobile.
//
// Example structure:
//   export { useProducts } from './inventory/useProducts'
//   export { useStockLogs } from './inventory/useStockLogs'

export {}
