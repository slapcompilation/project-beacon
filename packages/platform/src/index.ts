// The generated client. Browser-safe: nothing here reaches for `pg`, which only
// the test files import.
export { createClient, type Client, type ActionType, type FunctionType, type Json, type Rpc } from './client'
export * from './generated'
