{
  "name": "@beacon/services",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "lint": "echo 'services package — no lint needed'"
  },
  "dependencies": {
    "@beacon/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
